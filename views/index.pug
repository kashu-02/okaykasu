doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title おk粕　Web版
  body
    h1 おk粕　Web版
    h1 DEBUG MODE
    div
    if user
      a(href="/logout") #{user.username} をログアウト
    else
      a(href="/login") ログイン